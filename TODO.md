- [ ] Diagnose current Nuxt production build failure (Rollup plugin-inject parsing .vue as JS)
- [ ] Remove/avoid the misconfigured rollup inject behavior until build passes
- [ ] Add localization (Nuxt i18n) with locales/* and $t() usage
- [ ] Add global theme handling with multiple palettes (global config/store + plugin)
- [ ] Update layouts/pages/components to use theme + i18n keys
- [ ] Run: npm run dev and npm run build to verify

