version: '3.8'

services:
  # Development frontend
  fluxbooks-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: fluxbooks-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
      HOST: 0.0.0.0
      PORT: 3000
    command: npm run dev

  # Production-ready frontend (for testing)
  fluxbooks-prod:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fluxbooks-prod
    ports:
      - "3001:3000"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
    restart: unless-stopped
